import { Buttons } from "../Buttons/Buttons";
import { SearchBox } from "../SearchBox/SearchBox";

export const Header = () => {
  return (
    <header>
      <Buttons />
      <SearchBox />
    </header>
  );
};
